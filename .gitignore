# Build artifacts
build/
build-*/
.cache/
compile_commands.json

# Object / library files
*.o
*.a
*.so

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store
.buildcheck/

# Onion service runtime state (contains secrets - never commit)
onion/tor_data/
onion/tor.out
onion/tor.log
onion/tor.pid
onion/web.log
onion/torrc
onion/client.keys

# Encrypted snapshots / scratch
*.enc

